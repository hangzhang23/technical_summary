# 双指针框架总结
## 针对题目的问题可分为快慢指针和左右指针。
快慢指针主要处理链表问题，比如链表是否有环。而左右指针主要处理数组问题，比如二分查找。

### 快慢指针
#### LC141.环形链表
这道题非常经典的用到了快慢指针，设置两个指针，一个一次跳两步，一个一次跳一步。如果存在环形链表得时候，快指针总会追上满指针。
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

def hasCycle(self, head):
    fast = slow = head
    if not head: return False
    while fast and fast.head:
        slow = slow.next
        fast = fast.next.next
        if fast == slow: return True
    return False
```
